#include<stdio.h>
void main()
{
	int i=0,count=0;
	char str[40];
	printf("\nEnter the String:");
	scanf("%s",&str);
	while(str[i]!='\0')
	{
		if(str[i]=="[^n]")
		{
			++count; 
		}
		++i;
	}
	++count;
	printf("\n%d",count);
}
