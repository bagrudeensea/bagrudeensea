#include<stdio.h>
int main()
{
int array[99],m,i,minimun;
printf(" Enter the numbers");
scanf("%d",&m;
for(i=0;i<m;i++)
{
scanf("%d",&array[i]);
}
max=array[0];
for(i=0;i<m;i++)
{
if(max<array[i])
max=array[i]);
}
printf(" the minimum number in the array=%d",max);
getch();
  
}
